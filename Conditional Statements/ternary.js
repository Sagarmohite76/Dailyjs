
// 1.
let age = 20;
let canVote = (age >= 18) ? "You can vote" : "You cannot vote";
console.log(canVote);

// 2.
let number = 15;
let result = (number % 2 === 0) ? "Even" : "Odd";
console.log(result);

// 3.
let score = 85;
let grade = (score >= 90) ? 'A' : (score >= 80) ? 'B' : (score >= 70) ? 'C' : 'F';
console.log(grade);

// 4.
let isLoggedIn = false;
let status = isLoggedIn ? "Welcome back!" : "Please log in.";
console.log(status);

// 5.
let number2 = -5;
let result2 = (number2 > 0) ? "Positive" : (number2 < 0) ? "Negative" : "Zero";
console.log(result2);

// 6.
let ageGroup = 10;
let classification = (ageGroup < 13) ? "Child" : (ageGroup >= 13 && ageGroup < 20) ? "Teenager" : "Adult";
console.log(classification);

// 7.
let userRole = 'admin';
let access = (userRole === 'admin') ? 'Full access' : 'Limited access';
console.log(access);

// 8.
let user = null;
let username = (user != null) ? user.name : "Guest";
console.log(username);

// 9.
let score2 = 72;
let grade2 = (score2 >= 90) ? 'A' : (score2 >= 80) ? 'B' : (score2 >= 70) ? 'C' : 'F';
console.log(grade2);

// 10.
let Price = 120;
let discount = (Price > 100) ? "You get a 20% discount!" : "No discount available.";
console.log(discount);

